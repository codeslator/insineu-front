export enum PATHS {
  ROOT = '',
  HOME = 'home',
  AUTH = 'auth',
  USERS = 'users',
  TEACHERS = 'teachers',
  STUDENTS = 'students',
  ROLES = 'roles',
  PERMISSIONS = 'permissions',
}

export enum ROUTES {
  ROOT = '/',
  HOME = '/home',
  AUTH = '/auth',
  USERS = '/users',
  TEACHERS = '/teachers',
  STUDENTS = '/students',
  ROLES = '/roles',
  PERMISSIONS = '/permissions',
}

export enum MENU_LABELS {
  // ROOT = '/',
  HOME = 'Home',
  // AUTH = 'AUthentication',
  USERS = 'Users',
  TEACHERS = 'Teachers',
  STUDENTS = 'Students',
  ROLES = 'Roles',
  PERMISSIONS = 'Permissions',
}